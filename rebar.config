{cover_enabled, true}.
{cover_excl_mods, [sql_lex, sqlparse]}.

{deps, [{jpparse, {git, "https://github.com/walter-weinmann/jpparse.git", {tag, "0.3.0"}}}]}.
{deps_error_on_conflict, true}.

{eunit_opts, [
    {skip_deps, true},
    verbose
]}.

{src_dirs, [
    "src",
    "test"
]}.

{xrl_opts, [dfa_graph, verbose]}.

{yrl_opts, [verbose]}.
